pipeline:
  agent:
    any:
  environment:
    SECRETTEST: hola.txt
  stages:
    - stage: "cat hello world env"
      steps:
       - echo "env is ${SECRETTEST}"
    - stage: "view directory"
      steps:
       - sh """
            echo hola >> hola.txt
            ls 
            """
       

