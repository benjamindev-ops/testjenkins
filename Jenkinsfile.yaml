pipeline:
  agent:
    any:
  environment:
    SECRETTEST: "hola.txt"
  stages:
    - stage: "cat hello world env"
      steps:
       - echo "env is is ${SECRETTEST}"
    - stage: "cat hello world file"
      steps:
       - Get-Content "${SECRETTEST}"
