pipeline:
  agent:
    any:
  environment:
    SECRETTEST: hola.txt
 
  stages:
      - stage: "cat hello world env"
        steps:
         - echo "env is ${SECRETTEST}"

      - stage: "view directory"
        steps:
         - python """
              echo hola >> hola.txt
              import os
              hola_raw = os.popen("cat hola.txt").read()
              print(hola_raw)
              """


