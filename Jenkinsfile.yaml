pipeline:
  agent:
    any:
  environment:
    SECRETTEST: hola.txt
  stages:
    - stage: "cat hello world env"
      steps:
       - echo "env is ${SECRETTEST}"
    - stage: "cat hello world env"
      steps:
       - Get-Content hola.txt
